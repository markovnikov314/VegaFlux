# foundation Benchmark Smoke

`python -m vegaflux_py.benchmark_smoke` parses the five-row normalized golden fixture and writes machine-readable timing metadata. It is a CI smoke check, not a performance claim.
